# Abundance Solutions — environment template
# Copy this file to .env.local and fill in real values.
# NEVER commit .env.local (it is gitignored).

# Supabase (client-safe values only)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# PowerSync instance URL (from PowerSync Dashboard -> Connect)
VITE_POWERSYNC_URL=https://YOUR_INSTANCE.powersync.com

# WhatsApp conversion bridge
VITE_WHATSAPP_NUMBER=263719450765

# Domain (for CORS / production checks)
VITE_APP_DOMAIN=abundance.co.zw
